## V2.3.15（2026-09-05）

### Zero 对接密钥复制与显示

- Zero 对接的 API Token、回调密钥新增生成、复制和显示/隐藏操作，避免掩码字段无法复制。
- 回调地址新增一键复制，并在页面明确两端参数必须逐字一致以及推荐填写顺序。
- 复制功能兼容不允许 Clipboard API 的 HTTP/LAN 页面，会自动使用本地复制兜底。

## V2.3.14（2026-09-05）

### 账号连接状态与备注修复

- 账号列表增加 WebSocket 实时连接状态；账号已启用但连接断开时显示黄色“连接断开”，不再错误显示绿色“正常”。
- 账号详情中的连接状态会同步更新账号卡片，手动连接、断开及定时刷新后无需刷新整个页面。
- 扫码登录或更新凭证时保留已有账号备注，仅在账号原本没有备注时补充系统默认备注。
- 新增连接状态与备注保留回归测试；前端类型检查、生产构建和后端完整测试通过。

## V2.3.13（2026-09-04）

### 已上架商品跨账号复用

- 商品列表新增“加入素材”入口，可把任一已连接账号的已上架商品直接导入商品素材库。
- 自动提取标题、描述、图片、价格、库存和多规格；缺少图片尺寸时自动识别，避免发布时图片比例错误。
- 同一来源商品再次导入会更新原素材，不会重复创建，并保留手工修改过的素材名称和运费设置。
- 素材库支持保存、编辑和批量发布多规格商品；目标账号仍会独立预检类目、动态属性和发布地址。
- 备份与恢复新增来源商品和多规格素材数据；V36 数据库迁移仅扩展素材表，不影响已有素材和已发布商品。
- 前端类型检查与生产构建通过；后端完整测试 198 项全部通过。

## V2.3.12（2026-08-31）

### Zero 1.7.6 异步转单对接

- 商品新增“Zero 异步转单”发货方式，可按购买数量逐条收集买家提交内容并显示进度。
- 提交明细、Zero 请求状态和最终结果回复全部持久化，网络或进程中断后自动重试。
- Zero 回调使用 HMAC-SHA256、五分钟时间窗、事件去重及订单/买家/会话/明细四重校验。
- Zero 会话与普通 AI、关键词自动回复隔离，避免买家提交内容触发额外客服回复。
- 设置页新增 Zero 地址、API Token、回调密钥和连接测试，并明确提示在 Zero 中建立商品/规格项目映射。

## V2.3.11（2026-08-20）

### 修复扫码凭证更新重复重连

- 修复二维码确认后前端再次提交同一份 Cookie，导致账号重复重连和 `timeout of 30000ms exceeded` 的问题。
- 新增账号和已有账号凭证更新统一由后端二维码会话保存 Cookie，前端只等待确认并刷新页面状态。
- 连接恢复继续在后台执行，飞牛 OS 远程验证期间不会再被前端 30 秒请求超时打断。

## V2.3.10（2026-08-20）

### 飞牛 OS 无桌面安全验证

- Docker 运行镜像内置 Xvfb、x11vnc 和 noVNC；飞牛 OS 无需安装桌面环境即可完成闲鱼安全滑块。
- 启用 `CAPTCHA_BROWSER_REMOTE_ENABLED=true` 后，访问 `http://飞牛IP:7900/vnc.html?autoconnect=true&resize=scale` 操作验证。
- noVNC 验证画面只连接服务端浏览器，Cookie 仍保存在对应账号的独立 BrowserContext 中，三个账号不会互相串号。
- 增加远程验证启动检查和明确日志；支持自定义 noVNC 端口、绑定地址和 VNC 密码。

## V2.3.9（2026-08-20）

### 独立 JAR 验证浏览器兼容

- 修复独立 JAR 部署没有额外安装 Playwright Chromium 时，安全验证浏览器直接报“Playwright浏览器不可用”的问题。
- 浏览器启动依次尝试 Playwright Chromium、系统 Chrome 和系统 Edge；每个账号仍使用独立 BrowserContext，不共享 Cookie。
- 为浏览器候选项增加启动超时和明确日志，便于定位缺少浏览器或显示环境的问题。

## V2.3.8（2026-08-20）

### 扫码登录后自动恢复连接

- 修复账号此前因 `FAIL_SYS_USER_VALIDATE` 进入待验证状态后，扫码登录成功仍显示 WebSocket 未连接的问题。
- 扫码保存新 Cookie 后自动清理旧 Token 和内存验证等待状态，并复用凭证更新流程重建该账号 WebSocket。
- 增加回归测试，确保扫码登录成功会触发账号级连接恢复。

## V2.3.7（2026-08-20）

### 多账号安全验证闭环

- 为每个账号创建独立的 Playwright BrowserContext，避免多个账号共用 Cookie、设备状态或验证上下文。
- Token 接口返回 `FAIL_SYS_USER_VALIDATE` 时，在桌面模式自动打开该账号的验证页面并等待新的 `x5sec` Cookie。
- 验证成功后清理旧的 `x5secdata`、`x5sectag`、`x5step` 挑战标记，回写 Cookie、清理 Token 缓存并重试一次。
- 增加验证浏览器启用、无头模式、超时和全局并发配置；无头容器继续保留手动 Cookie 更新兜底。
- 统一扫码、登录态刷新和 Playwright 的浏览器 User-Agent，减少跨请求指纹不一致造成的误判。

## V2.3.6（2026-08-19）

### 安全验证与实时连接恢复修复

- 修复完成安全验证后账号状态过早恢复、旧 Token 继续复用以及 WebSocket 无法正确重连的问题。
- 更新同一账号 Cookie 后依次清理旧状态、刷新 H5 Token 和 WebSocket Token，并仅在 WebSocket 初始化成功后恢复账号状态。
- 平台仍要求安全验证时暂停自动重连，避免重复请求造成重连风暴或进一步触发风控。
- 验证入口优先打开平台返回的官方验证地址，并明确提示网页验证后仍需使用同一账号扫码更新 Cookie。
- 订单详情请求遇到 H5 Token 过期时自动刷新 Cookie 并重试，降低自动发货流程中断概率。
- Playwright 升级至 `1.61.0`，后端完整测试共 189 项通过。

## V2.3.5（2026-08-18）

### 自动发货流程与分段消息重构

- 卡券库编辑页新增清晰的发货流程预览，图片配置提升到流程第一步，不再隐藏在模板底部。
- 支持通过“添加下一条文字消息”自动创建分段消息，无需记忆 `######` 分隔符。
- 图片、卡密和联系方式可按顺序独立发送；分段文字之间增加发送间隔，降低连续消息被合并或丢失的概率。
- 保留旧模板、`######` 和 `{kmKey}` 兼容，不需要迁移数据库或重新配置已有卡券库。

## V2.3.4（2026-08-18）

### 图片回执超时导致卡密未发送修复

- 修复闲鱼不返回图片回执时，系统把已送达图片误判为失败并中止卡密发送的问题。
- 图片消息已写入 WebSocket 但 10 秒内未取得回执时，按已提交处理并继续发送卡密文字。
- 保留图片先发、卡密文字后发和图片后的发送间隔。

## V2.3.3（2026-08-18）

### 图片与卡密消息发送间隔修复

- 保持图片先发送、卡密文字后发送的顺序。
- 图片取得平台回执后增加发送间隔，避免卡密文字紧跟图片被闲鱼端丢弃。
- 自动发货和手动补发均适用，未配置图片时不增加额外等待。

## V2.3.2（2026-08-18）

### 图片消息比例修复

- 自动发货图片按图片真实宽高发送，修复手机端竖图变窄、底部出现大块空白的问题。
- 自动回复和网页客服发送图片也统一读取真实尺寸，不再使用旧版固定的 `800 x 800`。
- 图片尺寸读取失败时不再伪造正方形尺寸，避免错误比例继续传递到闲鱼客户端。

## V2.3.1（2026-08-18）

### 多账号卡券库发货图片

- 卡券库的自动发货图片支持一次勾选多个账号，并将同一张图片逐个上传到对应账号。
- 自动发货时按订单成交账号选择该账号自己的图片，避免多账号销售同一商品时图片发送失败。
- 可查看、替换或移除每个账号绑定的图片；原有单图片配置继续兼容，未单独配置账号仍可使用旧图片。
- 备份和恢复按账号 UNB 保存图片映射，避免账号 ID 变化后丢失绑定。

## V2.3.0（2026-08-13）

### 自动完整修复与二次扫码提示

- 每个正常账号每隔随机 5～8 小时自动执行一次完整连接修复，包括刷新 H5 Token、刷新 WebSocket Token、断开旧连接并重新连接。
- 多账号进入全局错峰队列，任意两个账号的自动完整修复至少间隔 1 小时，避免集中请求；账号较多时优先保证错峰安全。
- 容器重启后重新生成随机计划；禁用账号与等待安全验证的账号不会执行自动完整修复。
- 扫码添加账号和扫码更新凭证进入人脸验证时，增加醒目的红色“第二次扫码”提示。
- 前端类型检查、生产构建和后端完整测试通过，共 176 项测试。

## V2.2.20（2026-08-13）

### 人脸验证自动闭环

- 修复人脸验证完成后平台页面停在白屏、扫码弹窗无法获得验证结果的问题。
- 后端复用原扫码会话 Cookie，自动完成安全验证页面跳转、提取专用人脸二维码并展示在原弹窗中。
- 用户扫描人脸二维码并完成识别后，系统自动轮询验证结果、访问 `ivCheckLogin` 回调并收集最终登录 Cookie，无需再次扫描登录二维码。
- 自动链路提取失败时保留原安全验证链接与 Cookie 导入 / 更新入口作为兜底。
- 前端类型检查、生产构建和后端完整测试通过，共 172 项测试。

## V2.2.19（2026-08-13）

### 风控验证后二维码过期修复

- 修复扫码进入人脸或安全验证后，平台将一次性二维码标记为已过期，系统错误结束整个验证会话的问题。
- 账号进入安全验证后，即使后续二维码状态返回 `EXPIRED`，系统仍保留验证链接和原登录会话并继续等待结果。
- 安全验证的 15 分钟等待时间从进入验证状态时重新计算，不再沿用二维码生成时间。
- 保留普通二维码真正过期时的自动换码、手动重新生成和 Cookie 更新入口。
- 新增验证状态回归测试，防止已消费的二维码再次中断人脸验证流程。
- 前端类型检查、生产构建和后端完整测试通过，共 169 项测试。

## V2.2.18（2026-08-13）

### 凭证修复二维码过期自愈

- 修复重复打开扫码弹窗时短暂保留上一张过期二维码和旧状态的问题，每次打开都会先清空旧会话并等待新二维码生成完成。
- 二维码生成请求增加并发隔离，已关闭或已被新请求替代的响应不会覆盖当前二维码。
- 新二维码在 30 秒内被平台判定过期时自动重新生成一次，避免偶发的生成即失效。
- 二维码过期、取消、异常或生成失败后提供“重新生成二维码”和 Cookie 导入 / 更新入口，不再卡在过期页面。
- 识别接口外层成功但二维码内容为空的情况，显示明确错误并允许立即重试。
- 前端类型检查、生产构建和后端完整测试通过，共 167 项测试。

## V2.2.17（2026-08-13）

### 扫码风控验证与 Cookie 兜底

- 修复扫码触发人脸或安全验证后，后端监控和前端状态查询立即停止的问题；风控验证现在作为登录中间状态处理。
- 扫码会话最长等待时间延长到 15 分钟，验证期间持续查询同一平台会话，验证成功后可继续完成登录。
- 添加账号和更新凭证的扫码窗口都会显示安全验证状态，并提供“改用 Cookie 导入 / 更新”的直接入口。
- 明确提示浏览器验证页与服务器扫码 Cookie 可能无法互通；页面空白或验证后无结果时可直接使用浏览器 Cookie 完成登录。
- 前端类型检查、生产构建和后端完整测试通过，共 167 项测试。

## V2.2.16（2026-08-13）

### 扫码登录确认修复

- 修复闲鱼扫码后长期停留在“已扫码，等待确认”，无法完成账号添加的问题。
- 二维码登录全过程持续合并平台会话 Cookie，并保留确认跳转返回的最终登录信息。
- 兼容扫码状态字段、状态拼写和中间状态变化，未知中间态不再误判为用户取消。
- 风控验证、取消、会话异常和登录信息获取失败会显示明确结果，不再无限等待。
- 防止扫码状态请求重叠以及登录成功后的重复账号添加请求。
- 前端类型检查、生产构建和后端完整测试通过，共 166 项测试。

## V2.2.15（2026-08-11）

### 商品发布登录跳转修复

- 修复进入“新建 / 发布商品”时，闲鱼账号 Cookie 业务错误被误判为后台登录过期并跳回登录页的问题。
- 发布页默认选择第一个状态正常的账号，单账号和批量发布均不再选择已停用或异常账号。
- Cookie 不可用时保留当前后台登录状态，并提示先到账号管理更新凭证。
- 前端类型检查、生产构建和后端完整测试通过，共 162 项测试。

## V2.2.14（2026-08-11）

### 发布构建修复

- 修复 Docker 构建阶段未复制 `install.sh`，导致在线更新安装回归测试在容器构建中失败的问题。
- `v2.2.13` 的在线更新安装逻辑保留不变，本版本补齐容器构建验证。

## V2.2.13（2026-08-11）

### 在线更新安装

- 飞牛 OS/Linux 使用一键安装时，检测到 `systemd` 后自动部署宿主机在线更新代理。
- 首次安装代理不再重复构建和重启应用容器；已有部署仍可执行原命令单独启用。
- 未使用 `systemd` 或缺少管理员权限时保留明确的手动启用命令，不影响应用正常启动。

## V2.2.12（2026-08-11）

### 商品发布类目识别

- 类目推荐请求补齐闲鱼网页发布页使用的来源参数，避免接口成功但不返回有效分类。
- 支持从嵌套响应中识别 `catId`、`cid` 和 `categoryId` 等类目字段，不再只兼容单一返回结构。
- 标题未命中类目时自动使用商品详情重试；发布页同时携带已上传图片辅助平台识别。
- 首次识别不再发送空的动态属性列表，选择联动属性后才携带平台标签重新加载。
- 本版本不增加数据库迁移；前端类型检查、生产构建和后端完整测试通过，共 160 项测试。

## V2.2.11（2026-08-11）

### 在线更新与版本状态

- 在线更新完成后会自动清理历史 `SUCCESS` 状态，版本窗口不再长期显示 100% 进度条。
- 版本检查改用 GitHub 正式 Release 的轻量接口；网络暂时不可用时明确显示“暂未获取”，不再显示“未知版本”。
- 迁移和手动更新脚本修复 Docker 命令读取标准输入的问题，避免一键迁移在数据库备份后提前结束。

## V2.2.9（2026-08-06）

### 仓库迁移

- 项目已迁移到 `najiuwanan511/xianyu-Plus`。
- 版本检查与在线更新现在使用新仓库的 GitHub Releases。

## V2.2.8（2026-07-31）

### 多规格同步与账号备注入口优化

- 商品配置中的多规格发货区域始终显示，无需先识别到多个规格即可查看配置状态。
- 新增“重新同步规格”入口，单个商品可随时重新获取平台规格，并立即显示已识别规格数量。
- 规格同步失败或平台要求安全验证时，直接展示具体原因，方便定位未显示多规格的问题。
- 识别到多个真实 SKU 后，可为每个规格分别选择卡密库，未单独配置的规格继续使用商品默认卡券。
- 账号列表在账号号后直接显示当前备注；未设置时显示“添加备注”，点击即可进入账号资料修改。

### 兼容与验证

- 本版本不增加数据库迁移，不改变自动发货、自动确认发货、自动回复、自动评价和小红花业务规则。
- 前端类型检查、正式构建和后端完整测试通过，共 149 项测试。

## V2.2.7（2026-07-29）

### 凭证页一键修复连接

- 凭证弹窗顶部操作合并为一个“修复连接”入口，系统会先自动刷新 WebSocket Token 并重连。
- 自动修复未成功且不需要安全验证时，自动进入扫码更新流程；扫码成功后更新 Cookie、Token 并恢复连接。
- 平台要求滑块验证时，主按钮自动变为“继续验证并修复”；打开闲鱼 IM 完成验证并返回后，自动进入扫码更新。
- “直接扫码更新”和“手动更新 Cookie”收纳到“高级操作”，凭证查看、复制、展开及多账号备注与 UNB 提示继续保留。

### 兼容与验证

- 本版本不增加数据库迁移，不改变自动发货、自动确认发货、自动回复、自动评价和小红花业务规则。
- 前端类型检查、正式构建和后端完整测试通过，共 149 项测试。

## V2.2.6（2026-07-28）

### 商品标题关键字筛选

- 商品列表标题区域新增“搜索商品标题”，输入关键字即可筛选包含该文字的商品。
- 标题关键字可与账号和商品状态组合使用，筛选在数据库分页前完成，不会遗漏其他分页中的匹配商品。
- 停止输入 350 毫秒后自动查询，支持按回车立即查询和一键清空；手机端同步提供搜索入口。

### 兼容与验证

- 本版本不增加数据库迁移，不改变自动发货、自动确认发货、自动回复、自动评价和小红花业务规则。
- 前端类型检查、正式构建和后端完整测试通过，共 149 项测试。

## V2.2.5（2026-07-28）

### 飞牛 OS 两容器在线更新

- 版本详情新增“立即在线更新”，实时显示检查、下载、校验、任务排空、安装、重启和健康检查进度。
- 更新代理运行在飞牛 OS/Linux 宿主机的 `systemd` 中，Docker 仍只保留应用与 MySQL 两个主要容器。
- GitHub Release 自动构建并上传版本 JAR 与 `SHA256SUMS.txt`；宿主机下载后强制校验，文件不完整时不会安装。
- 安装前创建数据库备份并保留旧 JAR；新版本健康检查失败时自动恢复更新前 JAR。
- 下载和校验期间业务继续运行；进入安装阶段后暂停领取新的发货、确认发货、自动回复、评价和小红花任务，并等待已执行任务安全结束。
- 在线更新完成或失败后自动解除维护状态；原有持久化任务在应用恢复后继续处理。

### 安装与兼容

- 首次启用需要在项目目录执行 `sudo ./deploy/self-update/install-online-update.sh`，以后可直接从网页更新。
- `update.sh` 继续作为手动更新后备方式；手动源码更新会停用旧在线 JAR 覆盖，确保运行刚构建的版本。
- 本版本不增加数据库迁移，不改变自动发货、自动确认发货、自动回复、自动评价和小红花的业务触发规则。
- 前端类型检查、正式构建、Linux Shell 语法检查和后端完整测试通过，共 148 项测试。

```bash
cd ~/xianyu-Plus
./update.sh
sudo ./deploy/self-update/install-online-update.sh
```
## V2.2.4（2026-07-28）

### 多规格独立发货

- 商品管理的“自动化配置”新增多规格卡密映射；每个真实 SKU 可单独指定固定内容、本地卡密或外部 API 卡密库。
- 每个规格可配置后台显示名；商品重新同步后按真实 SKU 保留显示名和独立发货规则。
- 未单独指定的规格继续继承商品默认卡券，普通单规格商品的原有配置方式保持不变。
- 订单新增 `skuId` 持久化；实时付款、订单同步、自动重试和人工补发统一优先按真实 SKU 匹配，避免同名规格发错内容。
- 规格和卡密库均校验账号归属，防止多账号环境下误绑其他账号的卡密库。
- 已删除或不可用的旧卡密库规则会保留等待重新选择，不会因打开配置弹窗被意外覆盖。

### 兼容性与升级

- 新增 V32 数据迁移，仅补充订单 SKU 和规格显示名字段；不会清空账号、订单、卡密或现有自动化配置。
- 自动发货、自动确认发货、自动评价和小红花的触发规则保持不变。
- 前端类型检查、正式构建和后端完整测试通过，共 144 项测试。

```bash
cd ~/xianyu-Plus
./update.sh
```
## V2.2.3（2026-07-28）

### 账号停用与运行任务清理

- 修复账号处于“需要验证”或连接异常状态时无法点击“禁用账号”的问题；现在除已禁用状态外均可直接停用。
- 禁用后立即停止该账号的 WebSocket、心跳、Token 续期、Token 重试、自动重连、自动回复和待执行自动化任务，同时关闭启动后自动连接。
- 删除账号前完整清理连接、重连、续期和验证缓存，避免删除后仍有后台任务访问旧账号。

### WebSocket Token 错峰续期

- 删除所有账号连接满一小时后集中刷新 Token 的旧机制。
- 根据数据库中的真实 Token 到期时间安排续期，并在到期前约 65～80 分钟按账号随机错峰执行。
- 同一账号只允许一个 Token 刷新流程；账号已禁用、已删除或等待平台验证时停止刷新与重连。

### 平台验证与多账号保护

- 凭证页固定保留“打开闲鱼 IM”入口，并明确显示当前账号备注和 UNB。
- 移除临时滑块地址、“我已完成，检测并重连”和关闭窗口后使用旧 Cookie 自动重试的流程。
- 完成平台验证后，通过扫码或手动方式更新最新 Cookie；更新成功后系统刷新 Token 并恢复连接。
- 更新 Cookie 时校验 UNB，避免多账号环境下把其他账号的凭证写入当前账号。
- 同步更新通知渠道提示和 V2.2.3 内置更新说明。

### 兼容性与验证

- 自动发货、自动确认发货、自动评价和小红花业务触发规则保持不变。
- 后端 431 个源文件编译通过，139 项测试全部通过；前端类型检查和正式构建通过。
- 本版本不新增数据库迁移，不会清空账号、订单、卡密或自动化配置。

```bash
cd ~/xianyu-Plus
./update.sh
```
## V2.2.2（2026-07-27）

### WebSocket 安全验证流程

- 修复平台返回滑块验证后，旧 Token 刷新链仍每 300 秒重试并反复输出异常日志的问题。
- 进入安全验证状态后立即停止该账号的心跳、Token 定时刷新、Token 延迟重试和 WebSocket 自动重连；其他账号不受影响。
- 凭证页显示“需要安全验证”和平台验证地址，新增“立即验证”与“我已完成，检测并重连”。
- 用户完成滑块并关闭验证窗口后，页面自动检测一次验证结果；验证生效后自动刷新 WebSocket Token 并恢复连接。
- 同一次验证事件只通过通知渠道提醒一次；验证完成并成功获取新 Token 后自动清除提醒状态。
- 验证链接仅允许打开闲鱼、淘宝和阿里官方 HTTPS 域名。

### 验证

- 新增安全验证暂停、禁止五分钟重试、验证地址更新和单次通知专项测试。
- 本版本不新增数据库迁移，不影响账号、订单、卡密、自动发货、评价或小红花配置。

```bash
cd ~/xianyu-Plus
./update.sh
```
## V2.2.1（2026-07-27）

### WebSocket Token 续期与连接恢复

- 修复 V1.9.5 之后 Session 过期恢复被固定延迟、导致多个账号在每小时 Token 轮换后长时间离线的问题；首次自动续期改为随机 3～8 秒内启动。
- Cookie、Token 或重新连接失败时仅针对当前账号按 5、15、30 分钟退避重试；平台要求安全验证时停止自动重试，避免重复触发验证。
- 续期前清除旧 Token 过期标记，避免重新连接继续复用旧凭证；修复界面把未知过期时间显示为 2000-01-01 的问题。
- 凭证页新增“待续期、刷新 Cookie、刷新 Token、重新连接、等待重试、需要验证、成功”等真实状态，并提供“刷新并重连”操作。

### 订单同步降频

- WebSocket 在线时以付款和订单状态事件为主，不再定时请求待发货订单接口。
- WebSocket 离线时每 2 分钟执行一次待发货订单补偿，避免漏单；买家确认收货、退款和交易关闭等状态每 5 分钟低频核对一次，确保自动评价仍可触发。
- 补偿只同步当前账号，不恢复已失败或需要人工核对的历史发货任务。

### 验证与升级

- 新增在线/离线订单同步策略和 Session 自动续期专项测试。
- 本版本不新增数据库迁移，升级不会清空账号、订单、卡密或自动化配置。

```bash
cd ~/xianyu-Plus
./update.sh
```
## V2.1.1（2026-07-25）

### 自动化保护修复

- 修复自动发货、自动评价、小红花和商品擦亮共用同一个失败计数器的问题；四类自动化现在按账号 + 模块独立统计，不再因为几个互不相关的偶发失败错误暂停整个账号。
- 修复“连续失败”实际只累计、不清零的问题；自动发货成功、评价成功或收到正常业务回执、小红花成功、擦亮成功或正常跳过后，会立即终止对应模块的连续失败序列。
- 自动化保护仍在同一模块于配置时间窗口内连续达到阈值时生效，暂停原因会明确记录触发模块、连续失败次数和最后一次失败信息。
- 保留买家身份不一致、买家身份待核验、发送结果不确定、自提订单和黑名单买家的安全拦截；本次修复不降低卡密防错发要求。

### 恢复自动化与任务安全

- 点击“恢复自动化”前先检查账号启用状态、Cookie 和实时连接；连接已断开时会尝试重连，预检失败直接返回具体原因，不再出现按钮提示恢复成功后马上再次暂停。
- 恢复时只重新加入状态正常、非自提且未达到最大尝试次数的风险暂停任务；已失败、需人工核对、自提和尝试次数耗尽的历史任务不会自动重放。
- 恢复成功会清除该账号所有模块的旧内存失败窗口，新的失败统计从恢复后重新开始。
- 发货任务确认完成后会清除“自动发货”连续失败计数，避免成功订单与早先失败订单继续错误叠加。

### 界面与排障

- 账号管理桌面卡片直接显示自动化保护的真实暂停原因，包括触发模块、失败次数和最后错误；不再只依赖鼠标悬停查看通用提示。
- 恢复结果会显示实际安全恢复的待发货任务数量，便于确认队列是否重新启动。

### 测试

- 新增自动化失败按模块隔离、成功后清零和恢复后重新计数专项测试。
- 新增风险暂停任务恢复 SQL 测试，固定验证正常状态、非自提和最大尝试次数条件。
- 新增发货成功清除自动发货失败序列测试。
- 完整运行后端测试、前端 Vue/TypeScript 类型检查、前端生产构建和后端打包验证。

### 数据库

- 本版本不新增数据库迁移，继续使用 V30 数据结构；升级不会清空订单、卡密、账号或自动化配置。

### 升级

```bash
cd ~/xianyu-Plus
./update.sh
```
## V2.1.0（2026-07-25）

### 订单同步与自动发货修复

- 修复订单详情已经解析出买家 ID 和商品 ID，但写回订单时遗漏这两个字段的问题；同步后订单管理会立即显示真实买家、商品、金额和规格，不再只剩订单号。
- 历史同步先创建订单、随后收到付款 WebSocket 消息时，会把买家 ID、买家昵称、商品 ID、消息 ID 和会话 ID 合并进原订单，并安全恢复尚未执行的自动发货任务。
- 订单详情补全只填充空的买家 ID 和商品 ID，不覆盖付款消息已经记录的身份；消息买家与平台订单详情买家不一致时仍会停止发送，继续保留防错发保护。
- 自动发货、固定内容、库存卡密、手动领取新卡密补发和自定义内容发货统一在发送前刷新订单详情、重读数据库并核验买家。
- 历史订单缺少会话 ID 时，手动与自动发货会使用已经核验的订单买家 ID 构造会话，不再因订单只有基础字段而直接失败。
- 付款消息只恢复状态为空或安全跳过的订单；自提、发送失败、人工核对和身份异常订单不会被重复付款消息重新排队。
- 待发货轮询不再把已经失败的任务反复重排队，保留最大重试次数与人工核对语义。

### 确认发货与默认回复

- 修复确认发货任务查询 SQL 多出一个右括号，导致 MySQL 每秒报错并使持久化确认发货队列停摆的问题。
- 默认回复配置明确显示：“仅首次回复”按账号 + 商品 + 买家持续去重，跨日期不会重置；需要每次触发时选择“每条消息都回复”。

### 一键更新

- 更新脚本检测到 Dockerfile 等受 Git 管理的本地改动时，会先连同未跟踪文件保存到带时间戳的 Git stash，再以 fast-forward 方式拉取官方 main；不再因本地构建修补而直接中止。
- .env 和数据卷不会被放入代码备份或覆盖；更新完成后会提示查看本地备份的命令。

### 验证

- 新增 H2 MySQL 兼容模式 SQL 执行测试，直接准备并执行确认发货查询，避免注解 SQL 只经过 Mockito 而未被数据库解析。
- 新增订单详情买家/商品字段写回、历史订单付款消息合并、任务安全恢复、订单详情商品 ID 解析和买家身份不覆盖专项回归。
- 完整运行后端测试、前端 Vue/TypeScript 类型检查、前端生产构建、后端干净打包和 Docker 多阶段构建。

### 数据库

- 本版本不新增数据库迁移，继续使用 V30 数据结构；升级后重新同步订单即可补齐已有残缺订单。

### 升级

```bash
cd ~/xianyu-Plus
./update.sh
```

Docker Compose 部署：

```bash
cd ~/xianyu-Plus
git pull origin main
docker compose up -d --build
```

## V2.0.10（2026-07-25）

### 一键安装与更新修复

- 修复 Docker 后端构建阶段缺少 `vue-code/src`，导致 2.0.9 一键安装或更新在安全回归测试中报 `NoSuchFileException` 并中止的问题。
- 后端构建阶段从已完成前端检查的构建层复制前端源码，保留消息渲染安全测试；源码仅用于构建验证，不会进入最终运行镜像。
- `install.sh` 与 `update.sh` 继续使用原有一键命令，用户拉取最新 `main` 后可直接重新构建，无需手工修改 Dockerfile。
- 本版本不新增数据库迁移，已完成的 V30 数据结构会原样保留。

### 验证

- 完整运行前端 Vue/TypeScript 类型检查和生产构建。
- 完整运行后端测试与干净 JAR 打包。
- 验证 Docker 多阶段构建可在后端测试阶段读取前端安全检查源码。

### 升级

```bash
cd ~/xianyu-Plus
git pull origin main
./update.sh
```

首次安装仍使用：

```bash
git clone https://github.com/najiuwanan511/xianyu-Plus.git
cd xianyu-Plus
chmod +x install.sh
./install.sh
```

## V2.0.9（2026-07-25）

### 自动发货与卡密安全

- 发货 API 改为明确的“已确认、此前已发货、平台拒绝、结果不确定”四态结果；Cookie 或令牌过期、空响应和缺少可核验数据不会再提交卡密或伪造成功。
- 发货任务在第一条外部消息前持久化发送标记；租约失效、进程中断或发送后数据库写入失败时统一进入人工核对，避免另一线程接管后重复发货。
- 多段文字和多次发送共用一次外部尝试标记，第二条及后续消息仍实时校验租约，不会因重复写标记被误停。
- 配置的发货图片逐张统计真实回执；图片失败、部分发送或异常不再把整单标为成功。
- 外部 API 卡券增加请求令牌围栏，旧请求不能覆盖新请求；网络超时、HTTP 异常、空结果和超时 REQUESTING 任务统一转人工核对，不自动向供应商重复取卡。

### 自动回复、通知与凭证一致性

- “仅首次回复”运行时和历史迁移统一使用“账号 + 商品 + 买家”的 SHA-256 去重键；买家 ID 缺失时安全回退到会话 ID。
- 自动回复在外部发送前持久化防重标记并持续续租；部分发送、回执不确定或进程中断会保留去重占位并进入人工核对，不再自动重放可能已送达的内容。
- 下单通知按订单原子领取，只执行一次；普通订单、自提订单和并发重复交易卡片共用同一通知状态，历史订单升级后不会补发旧通知。
- `_m_h5_tk` 刷新纳入 Cookie、WebSocket Token 和人工凭证更新共用的账号级锁，防止旧凭证覆盖新凭证。

### 订单、评价与运营首页

- 自动确认发货从内存异步任务升级为数据库持久化队列，支持重启恢复、任务租约、失败重试、自提跳过与账号停用延期。
- 手动评价、批量评价和定时评价增加订单级原子占位，同一订单同一时刻只允许一个平台评价请求。
- 首页新增真实 `merchantActionOrderCount`：统计待发货、自提待交付、发货异常、人工复核和退款售后，严格排除买家待付款与已结束交易。
- 今日订单、今日成交额和今日交付数分别按真实下单、付款、发货/交付时间计算，不再受本地记录写入时间影响，也不再受订单列表第一页 20 条限制。

### 数据库升级

- 新增 Flyway 迁移 `V30__complete_automation_reliability.sql`，自动修复历史默认回复去重键、取消重复待回复任务，并增加 API 卡券请求令牌、订单通知状态和确认发货持久化任务字段。
- 升级时仍处于外部卡券 REQUESTING 的历史记录会转为“结果待核对”，避免未知供应商结果被自动重复扣卡。
- 历史订单通知状态标记为已处理，仅新订单事件进入通知队列。

### 验证

- 新增发货四态结果、租约围栏、多消息发送、自动回复去重、API 卡券请求令牌、通知原子占位、评价原子占位、首页真实统计和确认发货重启恢复专项回归。
- 发布前执行后端全量测试、前端 Vue/TypeScript 类型检查、前端生产构建、后端干净打包和 Git 差异检查。

### 升级

```bash
git pull origin main
./update.sh
```

Docker Compose 部署：

```bash
git pull origin main
docker compose up -d --build
```

Windows 用户可在项目目录拉取 `v2.0.9` 后重新构建并启动服务；首次启动请等待 Flyway 完成 V30 数据库迁移。升级前建议备份数据库与部署配置。

## V2.0.8（2026-07-25）

### 紧急修复

- 修复 Cookie、H5 令牌或接口令牌过期时仍可能被非空错误文字误判为发货成功的问题；平台操作失败现在统一返回失败，订单与卡密不会提交成功状态。
- 文字与图片消息统一等待闲鱼服务端回执；超时、非 200 回执或连接异常均视为未确认送达，不再出现“卡密已使用但买家未收到”的伪成功。
- 卡密消息未取得送达确认时转入“待核对”，不直接退回可用库存，避免不确定送达后再次发出同一张卡密。
- 已存在或重新识别出的自提订单始终保持 PICKUP 自提渠道，只更新为“自提待交付”，不会重新进入自动发货队列。
- 发货任务续租失败后旧执行线程立即失去发送权限；发送前、发送后、提交卡密和写入成功状态前均再次检查租约，防止另一线程接管后重复发货。

### 一致性与并发保护

- 自动回复任务增加租约续期；AI 生成较慢或续租失败时，旧任务不会继续发送或覆盖新任务结果。
- “仅首次回复”新增数据库唯一占位，严格按“账号 + 商品 + 买家”原子去重；待发送、发送中和成功持续占位，失败或取消时释放占位。
- 外部 API 卡券任务支持回收超时的 REQUESTING 状态；修复回收后被失败状态二次认领条件拦截的问题。
- Cookie、`_m_h5_tk`、WebSocket Token 与人工凭证更新统一使用账号级锁，避免并发刷新时旧凭证覆盖新凭证。
- 账号和卡券事务在捕获异常并返回失败时显式标记回滚，避免页面提示失败但数据库只完成一部分修改。

### 安全与隐私

- 全局确认框与提示框改用 DOM 文本节点渲染接口消息，移除动态 `innerHTML` 注入路径。
- 连接状态接口不再返回 Cookie、`_m_h5_tk` 或 WebSocket Token 明文；前端仅显示是否配置、状态和过期时间。
- 实时日志不再输出发货正文、卡密、买家消息、完整消息 JSON 或图片地址，只保留账号、订单、会话、长度和结果等排障元数据。
- 更新前端生产依赖，生产依赖安全审计结果为 0 条已知漏洞。

### 数据库升级

- 新增 Flyway 迁移 `V29__harden_automation_consistency.sql`，自动创建默认回复去重键和唯一索引，并安全回填历史有效记录。
- 应用启动时会自动执行数据库迁移；升级前仍建议备份数据库和部署配置。

### 验证

- 后端 104 项测试全部通过，包含令牌过期、平台回执、自提订单、租约守卫、卡券超时回收、凭证并发锁和首次回复去重专项回归。
- 前端 Vue/TypeScript 类型检查通过，生产构建通过；后端干净编译与打包验证通过。

### 升级

```bash
git pull origin main
./update.sh
```

Docker Compose 部署：

```bash
git pull origin main
docker compose up -d --build
```

Windows 用户可在项目目录拉取 `v2.0.8` 后重新构建并启动服务；首次启动请等待 Flyway 完成 V29 数据库迁移。

## V2.0.6（2026-07-25）

### 紧急修复

- 卡密与所有自动发货内容发送前，新增订单详情买家 ID 与触发会话买家 ID 的强制一致性校验。
- 买家 ID 缺失、订单详情查询失败或两者不一致时，立即停止发送；卡密不会领取、不会提交，也不会发往错误会话。
- 自动发货、旧入口、规则补发与手动自定义发货统一使用该校验；保留原有本地账号收件人硬拦截和黑名单拦截。
- 新增买家身份一致与不一致两项回归测试，防止同类错发再次发生。

### 升级

```bash
./update.sh
# Windows：拉取 v2.0.6 后重新构建并启动服务。
```
## V2.0.5（2026-07-25）

### 修复

- 修复手机端手动发货后订单同步仍停留在“待发货”的问题：详情同步现在扫描整份闲鱼订单响应，不再只读取单一字段层级。
- 兼容订单详情中不同位置的已发货、待收货、已确认收货、交易成功和发货时间字段；命中后立即回写交易状态与确认发货标记。
- 订单已在手机端完成发货时，订单管理与自动化中心不再继续显示“等待确认发货”。

### 升级

```bash
./update.sh
# Windows：拉取 v2.0.5 后重新构建并启动服务，再在订单管理同步订单。
```
## V2.0.4（2026-07-25）

### 修复

- 订单历史同步兼容闲鱼多种交易状态字段；“已发货、待收货、已确认收货、交易成功”等状态会正确映射并显示。
- 每次手动同步会对最多 30 笔尚未确认的订单调用详情复核；详情明确返回已发货或交易成功时，立即回写确认发货状态，小红花不再停留在“等待确认发货”。
- 平台返回“当前订单不能评价”不再被误判为永久“无需评价”；历史记录会恢复为可重新核验，已确认收货订单可再次执行“检查并评价”。
- 仅超过评价期限、平台明确不支持评价或无评价资格的订单保留为终态，避免无意义重复请求。

### 升级

```bash
./update.sh
# Windows：拉取 v2.0.4 后重新构建并启动服务；随后在订单管理点击“同步订单”。
```
## V2.0.3（2026-07-25）

### 紧急修复

- 新增卡密与发货内容的本地账号收件人硬拦截：买家 ID 若匹配任一已配置账号的 UNB 或设备用户 ID，立即停止发送并保留卡密，不再向本地账号错发。
- 自动发货、补发、任务队列和手动发货统一经过该校验；订单买家身份异常时需要人工核验后再处理。
- 新增 UNB 与设备用户 ID 两类回归测试，防止同类错发再次出现。

### 升级

```bash
./update.sh
# Windows：拉取 v2.0.3 后重新构建并启动服务。
```
## V2.0.2（2026-07-25）

### 修复

- 自动评价的批量与定时任务统一按请求间隔串行执行；待评价列表暂未命中的订单也不会绕过节流，降低连续请求造成限流或风控的风险。
- 评价状态新增“无需评价”终态：平台明确不可评价、超过评价期限等订单不会再次进入自动评价；已确认收货但仍可核验的历史记录继续保留重新核验能力。
- 自动化中心、异常中心与历史记录会自动归类旧状态；终态订单显示“无需评价”，并禁用补评价入口。

### 升级

```bash
./update.sh
# Windows：拉取 v2.0.2 后重新构建并启动服务。
```
## V2.0.1（2026-07-25）

### 修复

- 修复卡密自动发货、补发和手动发货在多会话场景中错误使用会话 ID 作为接收人 ID 的问题；现在消息始终发送给订单记录中的实际买家。
- 订单缺少买家 ID 时停止发送并保留卡密，不再猜测接收人；卡密使用记录同步保存实际买家 ID，便于后续核对。
- 已确认交易若被旧记录标记为“无需评价”，现在可重新向平台核验评价资格，不再被永久跳过。

### 升级

```bash
./update.sh
# Windows：执行项目内更新脚本，或拉取 v2.0.1 后重新构建并启动服务。
```
## V2.0.0（2026-07-24）

### 功能

- 升级多账号运营工作台：深蓝导航、浅色工作区、品牌鱼标记及统一操作色彩。
- 首页聚焦商家待处理事项、账号状态、未读消息、自动化异常、在售商品与可用卡密。
- 账号设置改为右侧抽屉；一键擦亮支持全部账号汇总、错峰执行与账号备注记录。
- 商品配置和账号设置使用固定头部、独立滚动内容区与固定保存栏。
- 卡券库可安全清理已使用卡密；商品发布只需勾选核对项即可确认发布。
- 实时日志支持最新置顶、暂停、刷新间隔、筛选、复制和新日志提示。
- 仪表盘重排为“经营指标、商家待处理订单、账号状态、今日提醒、交付趋势”：仅读取已同步真实数据，待付款不会计入商家待办。
- 左侧导航保留原有全部菜单和入口，改用深海军蓝底、闲鱼黄选中态、统一加粗线性图标。
- 品牌标记升级为原创闲鱼黄色圆角方块鱼图标；保留 XianYuPlus 2.0 与多账号卖家助手两行文案。
- 仪表盘升级为商业化经营看板：今日成交额为核心主卡，新增真实“今日订单”指标，并强化待办、账号状态、提醒和趋势摘要层级。
- 深蓝导航提升品牌区与菜单文字对比度，增强图标和分组标题清晰度，保留全部既有入口与功能。
- 版本详情弹窗新增版本记录下拉，可直接查看 V1.9.7、V1.9.8、V1.9.9 与 V2.0.0 的完整中文更新内容。

### 修复

- 商品默认回复按账号、商品、买家严格去重，待发送、发送中和成功均视为已占位。
- 自提订单保留在订单管理中，并按自提待交付状态展示。

### 升级

```bash
./update.sh
# Windows：双击 update.sh 对应的更新脚本，或使用 Docker Compose 拉取新镜像后重建服务。
```
# XianYuPlus 更新日志

本文件记录各正式版本的主要变化。项目介绍与安装说明请查看 `README.md`。

## V1.9.5（2026-07-24）

### 商品默认回复

- 商品配置新增“商品默认回复”，可设置文字、上传一张图片，或同时发送文字和图片；图片会先上传到当前账号的闲鱼图片服务。
- 默认回复仅在买家首次进入该商品会话时发送一次，避免后续连续咨询重复推送；之后继续按议价、关键词和 AI 规则处理。
- 商品列表和商品详情会显示默认回复已开启状态；备份导入导出也会保留默认回复配置。

### AI 自动回复开关

- 明确“启用本商品 AI 自动回复”的含义：关闭后本商品不会调用系统 AI；系统设置仅在商品开关开启后提供模型与全局提示词。
- AI 主回复与关键词 AI 润色均增加商品开关复核，避免任何调用路径绕过商品级开关。

## V1.9.3（2026-07-23）

### 批量一键擦亮

- 一键擦亮页新增“全部账号擦亮”，会筛选可用账号并按账号错峰执行；每个账号仍先同步在售商品，再逐件擦亮。
- 账号的同步、风控保护、失败记录和执行结果相互独立；某个账号失败或正在运行时，不会中断其他账号。

### 拼单/助力服务发布

- 当平台类目返回“拼单/助力”及交付周期、服务类型、计价方式等动态字段时，发布页会识别为服务表单并允许继续填写。
- 服务发布必须完整选择交付周期、服务类型和计价方式，系统按平台返回的动态字段提交；其他专项类目仍保持拦截。

## V1.9.2（2026-07-23）

### 自提订单回补

- 当闲鱼商家订单列表接口临时返回无权限时，手动同步会扫描本地近 30 天已保存的 WebSocket 交易卡片，补回带明确自提标记和订单 ID 的订单。
- WebSocket 实时交易新增识别 `onlyTakeSelf=true`；自提订单统一标记为 `PICKUP`，不会进入虚拟发货、手动发货或确认发货队列。
- 同步完成提示会显示从本地交易消息补回的自提订单数量，便于确认回补结果。

## V1.9.1（2026-07-23）

### 自提订单同步

- 修复已完成自提订单未进入订单管理的问题：兼容 `onlyTakeSelf=true` 标记、缺少商品 ID 的实时订单事件，以及缺少标准时间字段的最新订单列表记录。
- 自提订单会统一标记为 `PICKUP`，并移出虚拟发货、手动发货、确认发货等物流任务队列；已有记录再次同步后也会自动纠正。

### 账号管理响应式布局

- 账号管理改为按实际内容区宽度切换完整、紧凑和卡片布局，修复 13 英寸笔记本、分屏或浏览器缩放时账户卡片横向裁切的问题。

## V1.9.0（2026-07-22）

### 商品同步与稳定性

- 修复商品详情同步停留在 `0/1`、同步按钮持续转圈的问题；卡住或旧的详情任务会自动收口并释放账号同步状态。
- 修复异步详情同步的 Spring 代理类型错误。商品基础列表已保存时，即使详情补全未启动，也不会再误报账号连接异常。
- 商品售出下架后，“在售”分组为空会被视为正常同步完成；同步失败会展示具体账号与原因。

### 自提订单

- 自提订单会进入订单管理，并显示“自提待交接”。
- 自提订单不会进入自动发货队列，且禁止虚拟发货、手动发货和确认发货。
- 订单历史同步与实时交易消息都会识别自提标记，减少漏单与物流自动化误触发。

### WebSocket 与安全验证

- WebSocket Token 触发闲鱼网页安全验证后，会暂停该账号的自动重连，避免反复刷新 Cookie、重复请求 Token 与刷屏操作日志。
- 用户完成验证并更新凭证后再主动重连；闲鱼 App 不一定会弹出提示。

### 更新体验与构建

- 修复 WebSocket 安全验证提示的前端类型声明，恢复 Docker 前端生产构建。
- 更新弹窗优先显示 GitHub 最新正式 Release 的中文发布说明，不再展示开发提交标题；没有发布说明时才回退到提交摘要。

## V1.8.10（2026-07-22）

### 版本更新提示

- 更新弹窗优先读取 GitHub Release 的中文发布说明，不再优先展示开发提交标题。
- 当 Release 不可用或没有说明时，才回退显示提交摘要，保证更新说明始终可读。

## V1.8.9（2026-07-22）

### 构建修复

- 修复 WebSocket 安全验证提示新增 `message` 字段后遗漏前端 TypeScript 类型声明，恢复 Docker 前端生产构建。

## V1.8.8（2026-07-22）

### WebSocket 安全验证

- WebSocket Token 请求触发闲鱼安全验证后，立即暂停该账号的自动重连，避免反复刷新 Cookie、重复请求 Token 和刷屏操作日志。
- 安全验证等待状态不再自动超时清除；只有用户完成验证后通过“凭证更新”重新连接时才会继续请求。
- 连接页面明确说明这是网页安全验证，闲鱼 App 不一定会弹出提示，并给出完成验证后的恢复步骤。

## V1.8.7（2026-07-22）

### 商品与订单同步

- 修复详情同步异步代理注入为具体实现类时触发的 Bean 类型错误；商品基础列表已同步后，不会再被误报为账号连接失败。
- 当闲鱼“在售”分组为空（例如商品已售出下架）时，商品同步会正确完成并提示当前没有在售商品。
- 同步失败提示改为展示具体账号与后端返回原因，便于区分账号会话问题与业务同步问题。

### 自提订单

- 自提订单会写入订单管理，并标记为“自提待交接”。
- 自提订单不会进入自动发货队列，也不允许虚拟发货、手动发货或确认发货。
- 订单历史同步与实时交易消息均会识别自提标识，避免漏单或误触发物流自动化。

## V1.8.6（2026-07-22）

### 商品同步

- 修复商品列表执行“同步闲鱼商品”后，详情同步停留在 `0/1` 导致按钮一直灰色转圈的问题。
- 后端详情补全任务增加超时收口机制，卡住或旧任务残留时会自动释放账号同步状态。
- 前端同步进度增加兜底超时判断，长时间无进度时自动结束等待并恢复按钮可点击。
- 商品基础列表同步成功后，即使详情补全受闲鱼接口或页面加载影响，也会保留已同步的基础商品信息。

## V1.8.5（2026-07-21）

### 自动化与交付

- AI 客服回复延迟改为系统级可配置项，可在“系统设置 → AI 客服配置”设置 1–60 秒，默认 15 秒，保存后即时生效。
- 商品卡密自动发货新增“自动确认发货”开关；卡密完整发送后才会执行确认发货。
- 自动评价在待评价列表暂未返回订单时，改由闲鱼最终评价接口核验，减少需要在执行中心手动“检查并评价”的情况。

### 备份与恢复

- 扩展手动备份：账号 Cookie 与自动化设置、全部系统设置、关键词规则、通知渠道、擦亮计划、黑名单、买家标签和商品素材库均可导出与恢复。
- 备份页面增加敏感信息提示，并明确导入采用按标识新增或更新的方式，不会删除备份中不存在的旧记录。

## V1.8.1（2026-07-20）

### 界面与体验

- 运营总览的近 7 日与近 30 日交付数据改为平滑趋势图，更直观地查看每日变化。
- 将“发布商品”和“商品素材库”整理为同一组下拉导航，并调整运营中心菜单顺序。
- 系统公告移动到顶部左侧，减少与账号操作区域的视觉冲突。
- 删除订单列表、订单详情和自动化执行中心内影响布局的行内复制按钮；“更多操作”中的复制订单 ID 功能继续保留。

### 安装与更新

- Linux / 飞牛 OS 安装说明简化为一条可直接执行的安装命令。
- 移除需要额外 Docker 容器的网页在线更新功能，继续使用 `update.sh` 完成安全更新。
- 更新脚本会兼容既有数据卷，并清理此前在线更新功能可能遗留的容器和镜像。

### 文档与项目展示

- 重构项目介绍、功能说明与部署指引，让首次接触项目的用户更容易了解用途。
- 增加隐私处理后的主要功能界面预览。
- 保留交流与反馈入口，并继续强调非商业使用边界与平台风控提示。

### 质量验证

- 前端 TypeScript 类型检查与生产构建通过。
- 后端 61 项自动化测试全部通过。
## V2.2.10（2026-08-11）

### 商品发布与多规格

- 商品发布支持单维度多规格：可分别设置规格名称、售价、原价和库存，并将各规格提交到闲鱼 SKU 列表。
- 拼单/助力服务类目支持动态联动选择：先选择分类后，自动刷新交付周期、服务类型和计价方式；发布前会用全部已选字段重新校验。
- 优化闲鱼类目识别，兼容接口返回多个类目候选的情况。

### 自动交付与回复

- 多规格订单必须匹配当前商品的真实 SKU 与对应交付配置，缺失或不匹配时停止发货，避免错发。
- 自动发货和默认回复中的图片统一先于文字发送。
- 卡券交付支持图片内容；重新添加账号后，账号编号从 1 开始。

### 兼容与验证

- 不增加数据库迁移，不改变已有单规格商品的发布方式。
- 后端完整测试、服务类目联动回归测试、前端类型检查和生产构建通过。
